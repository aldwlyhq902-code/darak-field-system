<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug only. The emulator reaches the host machine at 10.0.2.2 over plain HTTP,
  so without this the out-of-the-box run fails on Android 9+ with a cleartext
  error that looks like a server problem.

  Release builds do not include this file, so production traffic stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
