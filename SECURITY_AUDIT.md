# تدقيق أمان الإصدار 0.2.0

**النطاق:** Laravel API/لوحة الإدارة، تطبيق Android، المزامنة، الأدلة، النسخ
الاحتياطي، إعدادات البناء والتوزيع. **التاريخ:** 2026-08-12.

## النتيجة

لا توجد ثغرة حرجة أو عالية معروفة مفتوحة في الشيفرة المدققة أو اعتمادات Composer
المقفلة. المشروع مناسب لمرحلة قبول ميداني مضبوطة، وليس للنشر الإنتاجي قبل نجاح
`darak:preflight` على بيئة الإنتاج ودورة هاتف حقيقية.

## ضوابط منفذة ومختبرة

- MFA/TOTP إلزامي للمالك والإداري؛ كلمة المرور وحدها لا تنشئ جلسة لوحة.
- أسرار TOTP مشفرة، ورموز الاسترداد hashes أحادية الاستخدام، ومحاولات الدخول
  والتحدي محدودة المعدل.
- صلاحيات API على الخادم مع اختبارات IDOR، وربط الرمز بالجهاز وإبطال الجهاز.
- CSP بـnonce، منع framing وMIME sniffing، HSTS على HTTPS، ومنع cache للوحة.
- SQLCipher لقاعدة الهاتف بمفتاح 256-bit من Android Keystore، مع ترحيل وrollback.
- `allowBackup=false` و`usesCleartextTraffic=false` في حزمة الإصدار.
- تحقق نوع/حجم/hash للأدلة، حد 25MiB، وحدود معدل لمسارات الرفع.
- نسخ احتياطي مشفر AES-256 في الإنتاج مع manifest وبصمات وتحقق قبل الاستعادة.
- بذر بيانات العرض مرفوض في `production`.
- حسابات العملاء بحارس مستقل وعزل حسب `client_id`، ولا تُخزن صفحاتهم الحساسة في PWA cache.
- روابط QR عشوائية مخزنة مشفرة/مجزأة وقابلة للتدوير، مع rate limit وhoneypot وحدود للصورة.
- عزل طلب الموعد والتقييم حسب `client_id`، والتحقق من تبعية الموقع والأصل، ومنع تقييم زيارة غير مغلقة أو إرسال تقييم ثانٍ.
- صلاحيات مفوض العميل بحسب الموقع والعملية، وصلاحيات مساحة لوحة الإدارة من الخادم لا من إخفاء الروابط فقط.
- سجل التدقيق append-only بسلسلة تجزئة؛ يمنع نموذج التطبيق UPDATE/DELETE وتمنعه PostgreSQL بمحفز قاعدة بيانات.
- الخصومات والتسويات المالية لا تطبق قبل اعتماد مستخدمين مستقلين عن مقدم الطلب وعن بعضهما.
- قفل صفوف قاعدة البيانات عند تحويل طلب الموعد وإرسال التقييم، لمنع التكرار عند الطلبات المتزامنة.

## أدلة التحقق

- اختبارات Laravel على PostgreSQL واختبارات Flutter والتحليل الساكن؛ العدد الملزم هو ملخص آخر CI ناجح على commit الإصدار، لا رقم منسوخ يدوياً هنا.
- CI يبني أصول الويب وAPK debug ويجري تحقق HTTP على `/up`؛ سيناريوهات MFA وIDOR والمزامنة داخل حزمة الاختبارات الآلية.
- تدقيق Composer: لا advisories معروفة.
- APK/AAB موقعة؛ بصماتها في `RELEASES/0.2.0.md`.

## مخاطر متبقية قبل الإنتاج

1. لا يوجد جهاز Android متصل؛ يلزم اختبار ترحيل قاعدة 0.1.0 ودورة وضع الطيران على
   هاتف فعلي. هذا اختبار قبول مادي، لا يمكن إثبات الكاميرا/الموقع من بيئة سطح المكتب.
2. صور الأدلة ملفات داخل sandbox التطبيق وليست داخل SQLCipher. يحميها عزل Android
   وتشفير الجهاز وتعطيل backup، لكن الجهاز المروّت خارج نموذج الحماية الحالي.
3. مفتاح التوقيع ذاتي الشهادة ومناسب للتوزيع الداخلي؛ يجب حفظ ملف keystore وكلمة
   مروره خارج المستودع وفي مدير أسرار، لأن فقده يمنع تحديث التطبيق المثبت.
4. فشل `darak:preflight` في بيئة التطوير الحالية متوقع (`local`, HTTP, SQLite).
   يجب أن ينجح بلا `FAIL` على الخادم الحقيقي قبل فتحه للمستخدمين.
5. Vercel سطح demo مؤقت فقط: نقطة الدخول ترفضه افتراضياً ولا تسمح به في
   `APP_ENV=production`. الأدلة الإنتاجية تحتاج قرصاً دائماً وخادماً ينجح عليه preflight.
