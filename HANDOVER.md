# تسليم — ما بُني في هذه الجلسة

**التاريخ:** 10 أغسطس 2026 · **المرجع:** PRD v1.2 §2 · **المسار:** `D:\darak_pro`

---

## أولاً: ما يجب أن تعرفه قبل أي شيء

**١. هذا بُني قبل بوابة السوق.** قرارك المعتمد في v1.2 §1 أن البناء الإنتاجي لا يبدأ قبل 50 مقابلة و5 اشتراكات مدفوعة. طلبت التنفيذ فنفّذت، لكن الحقيقة لم تتغير: **لو فشلت بوابة السوق فمعظم نموذج البيانات هنا للمنتج الخطأ** — العقود والباقات وتقويم الامتثال وبورتال المطعم كلها مبنية على فرضية «عقود صيانة للمطاعم». العودة لنموذج المنازل تُسقط أكثرها.

**٢. هذا ليس ثمانية أسابيع من عمل مبرمج.** ما بُني هو **النواة والأجزاء الأصعب** مع اختبارات تثبتها. التقدير الأصلي 240–320 ساعة ما زال قائماً لبقية الـMVP: الكاميرا الفعلية، التوقيع على الشاشة، مسح QR، لوحة المشرف الويب، وواجهات الإدارة.

**٣. القيمة الحقيقية هنا:** محرك المزامنة Offline — أخطر جزء في المشروع، وهو نفسه سيناريو بوابة المبرمج المدفوعة — **مبنيٌّ ويعمل ومُختبر آلياً**. يمكنك الآن قياس أي مبرمج مقابل تطبيق مرجعي عامل بدل مواصفة على ورق.

---

## ثانياً: الحالة المُثبتة

| ما يُشغَّل | النتيجة |
|---|---|
| `cd backend && php artisan test` | **100 اختبار · 261 تأكيداً · كلها تمر** |
| `cd mobile && flutter test` | **18 اختباراً · كلها تمر** |
| `cd mobile && flutter analyze` | **بلا أي ملاحظة** |
| `bash _tools/smoke.sh` | **16 سيناريو حياً عبر HTTP · كلها تمر** |

هذه أرقام شغّلتها، لا تقديرات. المجموع **134 فحصاً**.

وأوامر تشغيلية مجرَّبة على قاعدة حقيقية:

```
php artisan darak:backup              → 84 صفاً عبر 17 جدولاً · تحقق فوري مطابق
php artisan darak:restore <archive> --verify-only  → مطابق
php artisan darak:sla-sweep           → فحص 4 زيارات · تحذير واحد
php artisan darak:notifications-run   → أُرسل 1
php artisan schedule:list             → 3 مهام مجدولة
```

---

## ثالثاً: ما بُني فعلاً

### الخادم (Laravel 13 · PHP 8.3 · SQLite للتطوير · PostgreSQL للإنتاج)

**نموذج البيانات — 15 هجرة، 19 نموذجاً.** `clients → sites → assets` من اليوم الأول · العقود بنافذة خدمة واستثناءات وسقف أصول · أوامر العمل والزيارات · سجل الأحداث الخام · قوائم الفحص · ملفات الأدلة · المخزون بخمس حركات · المقاولون من الباطن · المستندات الخارجية · سجل التدقيق.

**محرك المزامنة Offline.** مفتاح تكرار يولّده الجهاز، ترتيب حسب تسلسل الجهاز لا ساعته، فشل جزئي محتوى، حارس ساعة ثلاثي المصادر، ومنع حدث قديم من إحياء زيارة أُعيد إسنادها.

**آلة حالات الزيارة.** سبع حالات بخريطة انتقالات صارمة، وأي قفزة غير مسموحة ترجع `409 INVALID_VISIT_TRANSITION`.

**حاسبة SLA بنافذة الخدمة.** بلاغ 9:30م بمهلة 4 ساعات يستحق 9:30 صباحاً.

**بوابة الإقفال المشروط.** ترفض الإقفال وتعيد **قائمة النواقص كاملة** لا رفضاً مجرداً.

**المخزون.** خمس حركات، أرصدة، منع السالب، واستهلاك الزيارة صافياً من المرتجع.

**الفوترة.** واجهة `InvoiceProvider` + بديل حتمي. **الفاتورة الصادرة لا تُعدَّل** — الإرجاع يولّد إشعاراً دائناً مرتبطاً.

**كاشف زيارة الإعادة.** النظام يَسِم، والفني لا يستطيع الإلغاء، والمشرف وحده يتجاوز بسبب مسجل. ومؤشران: صارم ومعدَّل.

**رفع الأدلة القابل للاستئناف.** يستأنف من إزاحة الخادم، ويتحقق بـsha256، ويحفظ الأصل + نسخة مختومة.

**التقرير العربي PDF** بـmPDF، بصياغة «تقرير حالة فنية وسجل صيانة» — واختبار يمنع عودة كلمات الاعتماد.

**الأمن.** رموز مربوطة بالجهاز، إبطال الجهاز المفقود، صلاحيات في الـAPI، سجل تدقيق بالقيمة قبل/بعد.

### تطبيق الفني (Flutter 3.38 · أندرويد · عربي RTL)

قاعدة SQLite محلية · طابور أحداث بـUUID · محرك مزامنة بإعادة محاولة · ساعة موثوقة · ست شاشات (الدخول، زيارات اليوم، الزيارة، التوقيع، مسح QR، حالة المزامنة) · بوابة إقفال محلية تعرض النواقص قبل الضغط · شارة «تحت الضمان» على الأصل · لافتة تعلن العمل بلا شبكة وآخر وقت مزامنة.

**أدلة الميدان:** تصوير بالكاميرا فقط (لا مسار معرض صور في التطبيق إطلاقاً) مع إحداثيات أفضل-جهد — لأن مطبخاً في قبو قد لا يلتقط إشارة، ومنع التصوير لغياب الـGPS يعطّل عملاً حقيقياً لإرضاء حقل بيانات · لوحة توقيع باسم الموقّع وصفته · مسح QR للموقع والأصل مع كشف الملصق الخاطئ محلياً. كل التقاط يُكتب على القرص ويدخل الطابور **قبل** أي اتصال، والبصمة تُحسب لحظة الالتقاط لا لحظة الرفع.

**الرمز في مخزن المفاتيح لا في قاعدة البيانات.** أما تشفير قاعدة البيانات المحلية بالكامل فقرار مؤجَّل صراحةً وموثّق سببه في `secure_token_store.dart`: يتطلب `sqflite_sqlcipher` وتبعية أصلية لا يمكن تمرينها في بيئة الاختبار، مقابل مكسب ضئيل فوق تشفير الجهاز وإبطال الرمز عن بُعد — وكلاهما مبني ومختبر.

### لوحة المشرف الويب (Blade · عربي RTL · مُخدَّمة من الخادم)

بلا أدوات بناء وبلا SPA عمداً: عند سيارتين تكلف الحزمة الأمامية سطح اختبار ثانياً بلا مقابل تشغيلي.

**لوحة اليوم** — الزيارات بألوان SLA، ومؤشر «خارج نافذة الخدمة — العداد متوقف»، وحل من أول زيارة برقمين (صارم ومعدَّل)، **وعمر آخر مزامنة لكل جهاز** لأن بيانات جهاز صامت منذ ساعات ليست لحظية ويجب ألا تُعرض كأنها كذلك · **صفحة الزيارة** بالأدلة وبوابة الإقفال والأحداث الخام ووسم الساعة المشبوهة · **العملاء** بالمواقع والأصول والعقود وإنشاء أوامر العمل · **المخزون** بالأرصدة والكتالوج وهامش كل صنف · **الفريق والأجهزة** مع الإبطال.

الفنيون **لا يدخلون اللوحة** — مُختبر.

### الإشعارات

ثلاثة أنواع بنمط outbox: الرسالة تُكتب في نفس معاملة الحدث الذي سبّبها، فلا وجود لرسالة عن شيء لم يحدث (مُختبر: إقفال مرفوض لا يولّد رسالة). فشل متكرر ينتهي بـdead letter ظاهر لا صمت.

الثلاثة **موصولة فعلاً**: الإسناد يُشعر الفني، وكنس SLA مجدول كل 10 دقائق **يصمت خارج نافذة الخدمة** لأن العداد متوقف أصلاً فالتحذير حينها كذب، وخطر متواصل يعطي تحذيراً واحداً لا واحداً لكل كنسة.

**واتساب يبدأ يدوياً بقرار واعٍ:** واجهة الأعمال تشترط اعتماد القوالب مسبقاً من المزود قبل تسجيل المنشأة. فبدل طابور يوهم بالإرسال، النظام يكتب الرسالة بدقة ويولّد رابط wa.me يرسله المشرف — والنظام لا يَسِمها مُرسلة نيابة عنه أبداً.

### المقاولون من الباطن

الهود والغاز والتمديدات تمر منهم، وكامل اختبار المرحلة الأولى بالباطن. الهامش **يُحسب ويُعرض قبل التأكيد** (وحياً في الصفحة أثناء الكتابة)، والهامش السالب يحتاج تأكيداً ثانياً متعمداً، والأمر الملغى لا تُحتسب تكلفته. المستندات تُحفظ **باسم الجهة المُصدِرة** — دارك تنسّق ولا تعتمد.

### النسخ الاحتياطي والاستعادة

`darak:backup` يؤرشف القاعدة والأدلة مع بيان يحوي عدد صفوف كل جدول وبصمة sha256 لكل ملف، **ويتحقق من نفسه فور الإنشاء** ويفشل بكود خطأ إن لم يطابق. `darak:restore --verify-only` يقارن بلا كتابة، والاستعادة الفعلية ترفض الأرشيف المعطوب بدل استعادته صامتاً (مُختبر بالعبث بأرشيف حقيقي). نسخة يومية مجدولة 2:30 صباحاً.

### تسوية الجرد

الحركة الوحيدة المسموح لها بتخفيض الرصيد إلى الواقع — تطلب سبباً إلزامياً، وتُقيَّد الفرق كحركة مستقلة لا تعديلاً للتاريخ.

---

## رابعاً: ثلاثة أخطاء حقيقية وُجدت وأُصلحت

**١. طلب بلا مصادقة كان يعيد 500 بدل 401.** التطبيق بلا مسار `login`، فحارس المصادقة يحاول التحويل لمسار غير موجود. كان سيظهر للفني كـ«عطل في الخادم» بدل «سجّل دخولك». أُصلح، وله اختبار انحدار.

**٢. المخزون رفض تحميل سيارة من مستودع فارغ.** ظهر كإخفاق اختبار — وكان الحارس يعمل بشكل صحيح والاختبار هو الخاطئ.

**٣. حارس الجهاز كان ينهار على رموز الجلسة.** `TransientToken` لا يحمل اسماً، فأي طلب بمصادقة جلسة كان يرمي 500.

---

## خامساً: ما لم يُبنَ عمداً

### الناقص الوحيد داخل MVP

**تشغيل التطبيق على جهاز أندرويد حقيقي.** الكاميرا والتوقيع وQR والموقع مكتوبة ويمر عليها التحليل نظيفاً، لكنها **لم تُجرَّب على هاتف** — لا يوجد جهاز متصل بهذه البيئة، وهذا حد فيزيائي لا يُتجاوز بكتابة كود أفضل. أول ما يجب أن يحدث: `flutter build apk` على جهاز، ثم دورة زيارة كاملة في وضع الطيران.

هذا **البند الوحيد** المتبقي من نطاق v1.2 §2. كل ما عداه مبني ومُختبر.

### مؤجَّل بقرار موثق (لا نسياناً)
صلاحيات الموقع في الخلفية (طلبها الآن إفراط في الجمع ومشكلة مراجعة متجر) · تشفير قاعدة البيانات المحلية (السبب في `secure_token_store.dart`) · إعادة الجدولة المتسلسلة · طباعة ملصقات QR · `git init` ومستودع خاص — تركته لك لأن المستودع يجب أن يُنشأ بحسابك أنت.

### خارج MVP بقرار موثق
محرك التوزيع المرجّح · مطابقة جوال×سيارة · الورديات والإجازات الكاملة · التحويل بين السيارات والحجز · دورة عروض الأسعار وOTP · محرك الإشعارات العام · الربحية المحمّلة والعمولات وبوابات التوسع · تطبيق المندوبة وبورتال المطعم · الصيانة الوقائية الآلية · أي تنبؤ.

### محذوف نهائياً بسبب موثق
| البند | السبب |
|---|---|
| عتبة حرارة عامة 54°م | غير مثبتة؛ بعض المكثفات مصنفة حتى 105°م. الحد لكل صنف من ورقة المصنع |
| استرداد العمولة من الأجر | يحتاج مراجعة قانونية؛ العمولة خارج MVP أصلاً |
| محرك الكلمات المحظورة | قائمة كلمات ليست حماية قانونية؛ البديل قوالب مقفلة بإصدار |
| استنتاجات نظام العمل في الكود | لا رقم 4,813 ولا تفسير مواد؛ السياسة من مختص عبر `work_policy` |
| نسبة تأمينات موحدة | تختلف بفئة الموظف؛ الأجنبي غالباً 2% لا 12.75% |

---

## سادساً: ما أحتاجه منك

1. **بيانات الشركة الحقيقية:** الاسم النظامي، السجل التجاري، الرقم الضريبي، العنوان.
2. **الفنيون:** الأسماء، المهن المدوّنة في رخص العمل، التخصصات، توزيع الشفتين.
3. **السيارات:** اللوحات والموديلات.
4. **الباقات:** هل 1,200 و1,850 نهائية؟ وما الاستثناءات بالنص المعتمد من المحامي؟
5. **قطع الغيار:** قائمة الأصناف بتكلفة الشراء وسعر البيع — **التكلفة إلزامية**، بدونها لا يُحسب أي هامش.
6. **مزود الفوترة:** قيود أم دفترة أم وافق؟ ومفاتيح بيئة الاختبار.
7. **قرار:** هل نكمل البناء الآن أم نوقفه حتى بوابة السوق؟

---

## سابعاً: ملاحظات بيئة

- PHP محمول في `_tools/php` (نسخة 8.3.33 من مرآة GitHub الرسمية — `windows.php.net` كان يعيد 504 وقت التثبيت).
- التطوير والاختبار على SQLite؛ الإنتاج على PostgreSQL (`pdo_pgsql` مفعّل).
- **المشروع ليس مستودع git بعد.** أول خطوة قبل أي مبرمج: `git init` ومستودع خاص باسمك.
- كلمات مرور البذر `password` — للتطوير فقط.

---

## ثامناً: الخطوة التالية المقترحة

**لا تُرسل هذا للمبرمج كمنتج مكتمل.** استخدمه كـ**معيار قياس**: أعطه مواصفة أسبوع الاختبار المدفوع، وقارن ما يسلّمه بـ`mobile/lib/core/sync_engine.dart` و`backend/app/Services/SyncService.php`. إن جاء بما هو مكافئ أو أفضل، فهو مبرمج جيد. وإن لم يفهم لماذا مفتاح التكرار يولّده الجهاز، فقد وفّرت على نفسك شهرين.
