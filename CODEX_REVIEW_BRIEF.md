# بريف مراجعة الكود المنفَّذ — لكودكس

انسخ ما بعد الفاصل والصقه في كودكس، ووجّهه إلى المستودع:
**https://github.com/aldwlyhq902-code/darak-field-system** (خاص — امنحه وصولاً، أو شغّله محلياً على `D:\darak_pro`).

---

أنت مراجع تقني **عدائي**. موقفك الافتراضي: كل ادعاء في هذه الوثيقة خاطئ حتى يثبته الكود. لا تلخّص ولا تمدح — اذهب لما هو مكسور.

## ما هو النظام

«دارك»: إدارة عمليات صيانة ميدانية لمطاعم صغيرة في جدة.
- **الخلفية:** Laravel 13 · PHP 8.3 · PostgreSQL إنتاجاً / SQLite تطويراً واختباراً — `backend/`
- **التطبيق:** Flutter 3.38 · أندرويد · **offline-first** — `mobile/`
- **اللوحة:** Blade عربية RTL — `backend/resources/views/panel/`

المسار الحرج: الفني يعمل يوماً كاملاً **بلا شبكة**، ثم يزامن دفعة واحدة.

## الحالة المُدّعاة

**136 اختبار خلفية · 30 اختبار Flutter · 16 فحصاً حياً عبر HTTP — كلها تمر · `flutter analyze` نظيف.**

هذه أرقام شُغّلت. **لا تعدّها دليلاً على الصحة** — مراجعة سابقة وجدت 49 عيباً بينما 134 اختباراً كانت خضراء، لأن الاختبارات فحصت الطرفين منفصلين ولم تعبر السيم بينهما ولا مرة.

## ما وجدته مراجعة سابقة وأُصلح — تحقق أن الإصلاح **صحيح** لا موجود فقط

سلسلة من أربعة عيوب جعلت **الزيارة غير قابلة للإقفال إطلاقاً** عبر المسار الحقيقي:

1. `media.register` كان يقرأ `checklist_instance_id` بينما التطبيق يرسل `asset_id` → كل صورة تُخزَّن بلا ربط، و`CloseGate` يطابق بالأول → `ASSET_PHOTO_MISSING` للأبد.
2. حالة الزيارة لم تكن تتقدم محلياً بلا شبكة — الكاتب الوحيد كان استجابة `bootstrap`.
3. المزامنة تدفع الأحداث قبل الصور → حدث الإقفال يصل والأدلة `pending`.
4. المرفوض يُوسم `failed` و`pending()` لا تختاره → لا إعادة محاولة آلية.

وأُصلحت أيضاً: صلاحيات مفقودة كلياً على المخزون والتقارير والوسائط · نسخ PostgreSQL احتياطية **فارغة من القاعدة** والتحقق يقول «مطابقة» · حذف عميل يمحو الأدلة · سباق TOCTOU يجعل المخزون سالباً · `on_site_seconds` يُحسب بوقت الخادم فينهار لصفر عند إعادة الدفعة · نافذة خدمة ليلية تلوّن كل زيارة حمراء · إشعار دائن يردّ ما لم يُحاسَب عليه.

## الأسئلة — وهذه نقاط ضعفي أنا

**١. ربط الصورة بالأصل (`SyncService.php` معالج `media.register` · `CloseGate.php`).**
أضفت عمود `media_files.asset_id`، و`CloseGate` يطابق بـ`asset_id` **أو** `checklist_instance_id`. افحص: صورة تُسجَّل بلا `asset_id` إطلاقاً (توقيع، أو صورة عامة) — هل تُرضي البوابة لأصل بالخطأ؟ وماذا لو وصل حدث الصورة **قبل** حدث قائمة الفحص في نفس الدفعة — هل `firstOrCreate` ينشئ نسخة فحص بلا حالة تمنع الإقفال بعدها؟ وهل يمكن لصورة أصل من زيارة سابقة أن تحتسب لزيارة جديدة؟

**٢. `on_site_seconds` من سلسلة الجهاز (`VisitStateMachine::elapsedSeconds`).**
أبحث عن آخر حدث `state.%.to.started` بـ`latest('id')`. تتبّع دفعة فيها **دورتان توقف واستئناف**: هل يلتقط الحدث الصحيح لكل فترة أم يخلط؟ وهل يعمل الاستدعاء داخل نفس المعاملة التي أنشأت الحدث؟ والسقف 16 ساعة — هل يبتر عملاً شرعياً طويلاً صامتاً بدل الإعلان عنه؟

**٣. المرآة المحلية لآلة الحالات (`mobile/lib/core/visit_progress.dart`).**
نسخت خريطة `Visit::TRANSITIONS` إلى Dart. **مصدرا حقيقة يمكن أن ينحرفا.** هل هناك حالة يقبلها أحدهما ويرفضها الآخر اليوم؟ وما تكلفة الانحراف على الفني؟ اقترح آلية تمنعه (اختبار عقد، توليد، أو نقطة API تصدّر الخريطة).

**٤. حجز الإقفال حتى اكتمال الرفع (`sync_engine.dart` · `visitsWithPendingUploads`).**
**أدخلتُ هنا عيباً واكتشفته بنفسي قبل ساعة:** رفع يفشل نهائياً كان يحجز الإقفال للأبد بلا مخرج. أصلحته بحصر الحجز على `pending`/`uploading`. **افحص الإصلاح:** هل بقي أي مسار يعلّق الإقفال بلا نهاية؟ وماذا لو فشل الرفع بعد إرسال الإقفال — هل تتعطّل الزيارة في `awaiting_close` بلا إشارة؟

**٥. القفل ضد السباق (`InventoryService::lockPartLocation`).**
`pg_advisory_xact_lock` **على PostgreSQL فقط**، والاختبارات كلها على SQLite — أي أن السباق **غير مختبَر إطلاقاً**. صمّم اختباراً يثبت أو ينفي كفايته على PostgreSQL. وهل يغطي `to_location_id` وحده حين يكون `from` فارغاً؟

**٦. `parts.declaration` و`CloseGate`.**
حوّلت شرط القطع من `every` إلى `contains` (أي نسخة فحص واحدة تحمل العلم تكفي). **هل أضعفت الضابط أكثر من اللازم؟** وهل تحديث كل نسخ الفحص دفعةً واحدة يمكن أن يُلغي إقراراً لأصل أُضيف لاحقاً؟

**٧. سعر العضو والإشعار الدائن (`InventoryService::visitConsumption` · `InvoiceService`).**
السعر يتبع `contract->isActive()` **وقت الحساب**. ماذا لو انتهى العقد بين الفوترة والإشعار الدائن؟ وهل `covered_move_ids` يمنع كل حالات الائتمان المزدوج، أم يوجد مسار يتجاوزه؟ وتحقق أن ضريبة الإشعارات الجزئية تجمع فعلاً إلى ضريبة الفاتورة في كل التقسيمات لا في الثلاثة التي اختبرتها.

**٨. `pg_dump` من داخل PHP (`BackupService::dumpDatabase`).**
`proc_open` بأمر مبني من الإعدادات. افحص الحقن رغم `escapeshellarg`، ودمج `$_ENV` (هل يضيع `PATH`؟)، وسلوكه عند دمب ضخم يفوق الذاكرة، وما إذا كان `--no-owner` يكفي لاستعادة على مستخدم مختلف.

**٩. الخروج عند 401/403.**
أي 403 يمسح الرمز ويُخرج الفني. هل يوجد 403 عابر (مثلاً `EnsureBackOfficeRole` على مسار يناديه التطبيق بالخطأ) يُخرج الفني وسط زيارة بلا سبب؟

**١٠. PDPL والخصوصية.**
النظام يحفظ إحداثيات فنيين وصور مواقع عملاء وتوقيعات. **لا سياسة احتفاظ ولا حذف ولا إشعار خصوصية.** ما الحد الأدنى الواجب قبل أول عميل حقيقي؟

**١١. ما زال ناقصاً.**
أي عيب جوهري لم تجده المراجعة الأولى ولا أنا. ركّز على: التزامن، الاسترجاع من الفشل، ما يحدث حين يمتلئ القرص، وما يحدث حين يعمل فنيان على نفس الزيارة.

## صيغة الجواب

1. **حكم في سطرين:** هل هذا الكود صالح لتشغيل تجريبي مع عميل حقيقي؟
2. **جدول:** `الخطورة | الملف:السطر | ما ينكسر عملياً | التصحيح`.
3. **إصلاحات ادّعيتها ولم تُصلَح فعلاً** — هذا أهم قسم عندي.
4. **اختبارات تعطي ثقة كاذبة** (تُرضي نفسها بدل فحص السلوك الحقيقي).
5. **ما تراه إفراطاً ويجب حذفه** لا تأجيله.

**قواعد:** لا تفترض حسن النية في تصميمي. أي بند يبدو ذكياً ولن يُستخدم في الميدان — قله. وإن كنت غير متأكد من حكم نظامي سعودي، قل «يحتاج تحققاً» ولا تخمّن.
