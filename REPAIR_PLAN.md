# خطة إصلاح نظام دارك — منفَّذة

> **حالة التنفيذ (10 أغسطس 2026): الموجات 0–5 كلها منفَّذة ومختبَرة — 49 ملاحظة، صفر متبقٍ.**
>
> الحصيلة: **136 اختبار خلفية · 28 اختبار تطبيق · 16 فحصاً حياً = 180**، كلها تمر · `flutter analyze` نظيف.
>
> | الموجة | الحالة |
> |---|---|
> | 0 — الجرح القاتل (5) | ✅ الزيارة تُقفل عبر المسار الحقيقي |
> | 1 — الأمن (3) | ✅ مغلقة باختبارات سلبية |
> | 2 — الفساد الصامت (4) | ✅ النسخ ترفض أرشيفاً بلا قاعدة · الأدلة محمية · قفل تزامن · الساعات من الجهاز |
> | 3 — الحسابات (4) | ✅ نافذة ليلية · إشعار دائن مزدوج · السعر المفوتر · فوترة غير مكررة |
> | 4 — صلابة التطبيق (4) | ✅ مهلات · 401 · حماية الزيارة الجارية · تطابق البوابة |
> | 5 — التشغيلية (18) | ✅ كلها |
>
> **تفصيل الموجة 5:** صلاحيات اللوحة (المالك وحده يبطل الأجهزة) · مفتاح الإشعار لكل إسناد وكل نوبة خطر · `sla-sweep` بأفق وتقسيم · نافذة bootstrap تشمل زيارة الأمس المفتوحة · سعر العضو يُطبَّق فعلاً · الأرصدة باستعلام واحد · مفتاح الباطن وأرقام أوامره · ضريبة الإشعار الدائن تناسبية · حركة مكررة = لا عملية لا خطأ · طوابع UTC · منع تداخل المزامنة · مزامنة تلقائية عند عودة الشبكة · تنظيف الأدلة المرفوعة · إعداد cleartext للتصحيح فقط · تصحيح دليل النشر (لا عامل طوابير).
>
> **اختبارات جديدة:** `SeamTest` · `ApiAuthorizationTest` · `OnSiteDurationTest` · `DataIntegrityTest` · `RemainingFindingsTest` · `offline_progression_test.dart`.

---

# الخطة الأصلية

**تاريخ المراجعة:** 10 أغسطس 2026 · **المنهج:** 5 مراجعين مستقلين + 3 محققين عدائيين + تحقق يدوي مباشر من الأربع الحرجة
**النتيجة:** 49 ملاحظة، بعد إزالة التكرار **~40 عيباً مؤكداً**

---

## الخلاصة قبل أي تفصيل

> **التطبيق لا يستطيع إقفال زيارة. لا offline ولا online.**

أربعة عيوب تتسلسل لتنتج هذا، وكلها فُحصت يدوياً في الكود لا بالثقة في وكيل:

1. حالة الزيارة لا تتقدم محلياً بلا شبكة → الفني عالق عند أول زر.
2. `media.register` يهمل `asset_id` فيخزّن `checklist_instance_id = null`، و`CloseGate` يطابق بالثاني → **كل** أصل يرجع `ASSET_PHOTO_MISSING` للأبد.
3. المزامنة تدفع الأحداث قبل الصور → حدث الإقفال يصل والأدلة `pending` → رفض.
4. الحدث المرفوض يُوسم `failed`، و`pending()` لا تختاره → لا إعادة محاولة آلية أبداً.

**ولماذا فات هذا على 134 اختباراً؟** لأن الاختبارات فحصت الخادم وحده والتطبيق وحده. `VisitLifecycleTest::satisfyCloseGate()` يُدخل صف `MediaFile` **بـ`checklist_instance_id` مضبوط يدوياً** بدل المرور عبر `media.register` الحقيقي — فأخفى العيب بدل كشفه. الاختبارات كانت تقيس الأجزاء ولا تعبر السيم بينها.

**قاعدة ملزمة لكل إصلاح في هذه الخطة: اكتب الاختبار الفاشل أولاً، ثم أصلح.** أي إصلاح بلا اختبار يفشل قبله لا يُقبل.

---

## الموجة 0 — الجرح القاتل (لا شيء يعمل قبلها)

### 0.1 حالة الزيارة لا تتقدم محلياً
`mobile/lib/app_state.dart:143-151`

`record()` يضع الحدث في الطابور فقط ولا يمس جدول `visits`. الكاتب الوحيد لـ`visits.state` هو `LocalDb.replaceVisits` وهو لا يعمل إلا بعد bootstrap ناجح — أي بشبكة.

**الأثر:** الفني بلا شبكة يضغط «انطلاق»، فلا يتغير شيء. الأزرار التالية غير موجودة أصلاً لأن `_actions` تُشتق من `_visit['state']`. وإن أعاد الضغط، يدخل حدث ثانٍ مكرر يُرفض لاحقاً `INVALID_VISIT_TRANSITION`.

**الإصلاح:** تحديث متفائل محلي عند `visit.transition` — انسخ خريطة `Visit::TRANSITIONS` إلى الجوال للتحقق قبل الكتابة، واكتب الحالة الجديدة في `visits`، ثم صالِحها مع `visits[]` القادمة في رد المزامنة (`SyncService::canonicalVisits` يعيدها أصلاً — استُهلكت في `bootstrap` فقط ولم تُستهلك في `sync`).

**معيار القبول:** اختبار Flutter: افتح زيارة `scheduled`، نفّذ الدورة الكاملة إلى `completed` **مع عميل HTTP يرفض كل الطلبات**، وتأكد أن `visits.state` تساوي `completed` محلياً وأن الطابور فيه 4 انتقالات.

### 0.2 الصور لا تصل بوابة الإقفال أبداً
`backend/app/Services/SyncService.php:210` · `backend/app/Services/CloseGate.php:49`

الجوال يرسل `asset_id` (كتبته في `evidence_store.dart:111`)، والخادم يقرأ `$payload['checklist_instance_id'] ?? null` — دائماً null. و`CloseGate` يطابق `->where('checklist_instance_id', $instance->id)`.

**الإصلاح المفضل:** في معالج `media.register`، اشتقّ نسخة قائمة الفحص من `asset_id`:
```php
$instanceId = null;
if (isset($payload['asset_id'])) {
    $instanceId = ChecklistInstance::firstOrCreate(
        ['visit_id' => $visit->id, 'asset_id' => (int) $payload['asset_id']],
    )->id;
}
```
**بديل أمتن:** أضف عمود `asset_id` على `media_files` واجعل `CloseGate` يطابق به — أقل هشاشة من الاعتماد على ترتيب وصول الأحداث.

**معيار القبول:** اختبار Feature يقود المسار الحقيقي كاملاً: `media.register` بـ`asset_id` ← رفع chunk ← `complete` ← محاولة إقفال ← **تنجح**. هذا الاختبار هو الذي كان مفقوداً.

### 0.3 ترتيب المزامنة يرفض كل إقفال offline
`mobile/lib/core/sync_engine.dart:88`

`_pushEvents()` قبل `_pushMedia()`.

**الإصلاح:** احجز أحداث `visit.transition → completed` في العميل حتى تصبح كل `pending_media` لتلك الزيارة `state='complete'`. (بديل أضعف: اقلب الترتيب — لكنه يفشل حين يكون الرفع بطيئاً والأحداث كثيرة.)

**معيار القبول:** اختبار Flutter: زيارة كاملة offline بصورتين وتوقيع، ثم `sync()` واحدة → لا يُرسل حدث الإقفال قبل اكتمال الرفع، وبعده يُرسل ويُقبل.

### 0.4 الحدث المرفوض لا يُعاد أبداً
`mobile/lib/core/event_queue.dart:172` مقابل `:148`

`markFailed` تضع `status='failed'` و`pending()` تختار `'pending'` فقط.

**الإصلاح:** فرّق بين رفض **دائم** (`INVALID_VISIT_TRANSITION`) ورفض **مؤقت** (`VISIT_CLOSE_BLOCKED` — يزول باكتمال الرفع). المؤقت يبقى `pending` بتراجع أُسّي؛ الدائم فقط يصير `failed`.

**معيار القبول:** رفض بـ`VISIT_CLOSE_BLOCKED` ثم اكتمال الرفع ثم `sync()` → يمر بلا تدخل يدوي.

### 0.5 إقرار القطع مشتق من حالة الأصل
`mobile/lib/screens/visit_screen.dart:241-248`

`noPartsUsed: status == 'ok'` — ولا يوجد أي منتج لحدث `part.issue` في التطبيق كله.

**الأثر مزدوج:** أصل بحالة «عطل» يمنع الإقفال للأبد؛ وأصل «سليم» بعد تبديل قطعة يسجّل «لم تُستخدم قطع» — وهو بالضبط تسريب مخزون السيارة الذي وُجدت البوابة لمنعه.

**الإصلاح:** افصل الإقرار عن الحالة. شاشة صرف قطعة تُنتج `part.issue` (بمسح QR القطعة الموجود أصلاً)، ومفتاح صريح «لم تُستخدم قطع» على مستوى الزيارة.

---

## الموجة 1 — ثقوب أمنية (كل نقطة استغلالها سطر curl واحد)

| # | الملف | الثقب | الإصلاح |
|---|---|---|---|
| 1.1 | `Api/InventoryController.php` | **لا تحقق صلاحية إطلاقاً.** فني يستطيع `POST /inventory/receipt {qty:9999}` فيضخّم المخزون والتقييم، ويقرأ أرصدة كل سيارة | اقصر الكتابة على owner/admin؛ واقصر قراءة الأرصدة على موقع سيارة الفني نفسه |
| 1.2 | `Api/ReportController.php` | `visits.csv` و`stock-moves.csv` مفتوحة لأي حامل رمز → تسريب كل العملاء والتكاليف وحركات المخزون | `abort_unless(isOwner() \|\| isAdmin())` — تماماً كما في `visitPdf` |
| 1.3 | `Api/MediaController.php` | لا فحص ملكية. فني أُعيد إسناد زيارته يستطيع **زرع أدلة مزوّرة**: يرفع بايتات من اختياره ثم يرسل sha256 لها فيمر الفحص | `$this->authorize('view', $media->visit)` في الثلاثة (status/chunk/complete) |

> ملاحظة على 1.3: فحص البصمة لا يحمي هنا لأن المهاجم يملك البايتات والبصمة معاً. الحماية الوحيدة هي الملكية.

---

## الموجة 2 — فساد بيانات صامت (الأخطر لأنه لا يصرخ)

### 2.1 نسخ PostgreSQL الاحتياطية **فارغة من قاعدة البيانات** — والتحقق يقول «مطابق»
`backend/app/Services/BackupService.php:78`

البيان يعلن `database/dump.sql` لكن **لا شيء يضيفه للأرشيف** في مسار pgsql. و`verify()` لا تفحص وجود المدخل المعلن. النتيجة: كل ليلة تطبع «تحقق فوري: مطابقة كاملة» وترجع 0، وبعد شهور يُفقد القرص فتستعيد الصور بلا صف واحد من البيانات. **RPO الحقيقي = ∞، وعلامات خضراء طوال الطريق.**

**الإصلاح:** نفّذ `pg_dump` فعلياً داخل `create()` وأضفه للأرشيف ببصمته؛ واجعل `verify()` **تفشل** إن كان `manifest['database']['entry']` معلناً وغير موجود. وصحّح `DEPLOYMENT.md` §5 التي تطلب خطوة `pg_dump` منفصلة بلا تنسيق.

**معيار القبول:** اختبار يبني بياناً بمدخل قاعدة معلن ثم يحذفه من الأرشيف → `verify()` ترجع `ok=false`.

### 2.2 حذف عميل واحد يمحو سجل الأدلة والتدقيق كاملاً
`migrations/2026_08_10_100100` وسلسلة `cascadeOnDelete` نزولاً

`forceDelete()` على عميل (أو `DELETE` يدوي) يدمّر بالتتابع: `visit_events` (سجل المزامنة ووسوم التلاعب بالساعة) · `media_files` بـ`original_hash` · `checklist_instances`. والفواتير تبقى في `external_documents` بـ`client_id` مُفرَّغاً — **سجلات مال بلا أي دليل مساند**، وهو بالضبط ما يُطلب في نزاع فوترة.

**الإصلاح:** حوّل جداول الأدلة والتدقيق إلى `restrictOnDelete`، وأضف حارس `forceDeleting` على `Client`/`Site`/`Asset` يرمي استثناءً.

### 2.3 سباق TOCTOU يجعل المخزون سالباً
`backend/app/Services/InventoryService.php:223`

طلبان متزامنان بمفتاحَي idempotency **مختلفين** يقرآن نفس الرصيد قبل الإدراج. المفتاح يحمي من إعادة نفس الحدث لا من حدثين. تحت PostgreSQL READ COMMITTED كلاهما يمر.

**الإصلاح:** `pg_advisory_xact_lock(hashtext(part_id || ':' || location_id))` أعلى المعاملة، أو جدول `stock_balances` بـ`UPDATE ... SET qty = qty - ?` وقيد `CHECK (qty >= 0)`.

### 2.4 `on_site_seconds` ≈ صفر عند مزامنة يوم كامل
`backend/app/Services/VisitStateMachine.php:61-64`

الحساب `state_changed_at->diffInSeconds($now)` — كلاهما وقت خادم. يوم offline يُعاد في ملّي ثانية فيصير الفرق صفراً. التقرير العربي يطبع «زمن العمل في الموقع: 0 دقيقة»، والـCSV مثله.

**وهذا ليس تجميلياً:** الساعات المفوترة هي أساس نموذج الإيراد كله في دراسة الجدوى.

**الإصلاح:** احسب الفرق من سلسلة `device_timestamp` للأحداث، مع تقييده بانحراف `ClockGuard` لمقاومة التلاعب، وارجع لوقت الخادم للانتقالات المتصلة فقط.

---

## الموجة 3 — حسابات ومال

| # | الملف | العيب | الإصلاح |
|---|---|---|---|
| 3.1 | `SlaCalculator.php:92` | نافذة ليلية (15:00–02:00 — مقبولة في المخطط ومعقولة لمطعم) تجعل `windowMinutesPerDay ≤ 0`: `dueAt` تسقط لساعة الحائط، و`remainingMinutes` ترجع 0 فتصير **كل** زيارة حمراء لحظة إنشائها | طبّق نفس الحارس في `elapsedMinutes`/`isWithinWindow`، ثم إما تنفيذ حساب النافذة العابرة لمنتصف الليل أو **رفضها عند التحقق من العقد** |
| 3.2 | `InvoiceService.php:102` | إرجاع سبق الفوترة (فخُصم أصلاً من مبلغها) يمكن أن يولّد إشعاراً دائناً عند إعادة إرسال الطلب — ائتمان للعميل عن بضاعة لم تُحاسَب | ارفض الإرجاع الذي `created_at` سابق لـ`issued_at` الفاتورة، وضع سقفاً تراكمياً للإشعارات الدائنة بقيمة الفاتورة |
| 3.3 | `InvoiceService.php:120` | الإشعار الدائن يستخدم `sale_price` **الحالي** لا السعر المفوتر فعلاً؛ و`member_price` مخزّن ومبذور ولا يُستخدم في أي حساب | خزّن سعر الوحدة المفوتر على بند الفاتورة واستخدمه في الإشعار |
| 3.4 | `InvoiceService.php:36` | `invoiceVisit` بمفتاح UUID جديد كل مرة → ليست replay-safe افتراضياً | اشتق المفتاح من `visit_id` |

---

## الموجة 4 — صلابة التطبيق

| # | الملف | العيب | الإصلاح |
|---|---|---|---|
| 4.1 | `api_client.dart` | **لا مهلة على أي طلب.** اتصال نصف مفتوح (تبديل برج، واي فاي أسير) يعلّق `sync()` للأبد؛ `_running` يبقى true فتصير كل مزامنة لاحقة عملية فارغة، والزر معطّل بـ«جارٍ المزامنة» بلا نهاية. التطبيق ميت حتى إغلاق قسري | `.timeout(Duration(seconds: 30))` (أطول للرفع) مُحوَّلة إلى `ApiException(0,'NETWORK')` فيلتقطها مسار إعادة المحاولة القائم |
| 4.2 | `sync_engine.dart:167` | خطأ 401 واحد يَسِم **كل** الطابور `failed` — والحدث بريء، الجلسة هي التي انتهت. ولا يوجد في التطبيق أي مسار خروج أو مسح رمز، فكل مزامنة لاحقة 401 وتُفشِل ما أُعيد | عالج 401/403 كفشل **دفعة** لا حدث: أبقِ الأحداث pending، امسح الرمز بـ`SecureTokenStore.clear()`، وجّه لشاشة الدخول |
| 4.3 | `local_db.dart:160` | `replaceVisits` تحذف زيارة الفني الجارية إن لم ترجع في bootstrap (تجاوز منتصف الليل) → «الزيارة غير موجودة على الجهاز» وسط العمل، والأدلة والتوقيع غير قابلة للالتقاط | لا تحذف زيارة لها صفوف في `pending_events`/`pending_media`/`local_checklists`؛ ووسّع نافذة bootstrap خادمياً لتشمل زيارات الأمس غير المقفلة |
| 4.4 | `visit_screen.dart:92` | البوابة المحلية أضعف من الخادمية (تقبل صورة واحدة للزيارة كلها بينما الخادم يطلب لكل أصل) → «جاهز» محلياً ثم رفض | طابق المنطق بدقة، أو اجعل الخادم يرجع البوابة عبر `/close-blockers` والتطبيق يعرضها |

---

## الموجة 5 — تشغيلية وتنظيف

- `BackupService.php:72` — نسخ ملف SQLite أثناء الكتابة قد يلتقط نسخة ممزقة؛ استخدم `VACUUM INTO`.
- `NotificationService.php:38,57` — مفتاح `visit.assigned` لكل (زيارة، فني) فلا يُشعر الفني عند إعادته؛ و`sla.at_risk` لكل زيارة للأبد فلا يحذّر بعد إعادة الفتح. أضف بُعداً زمنياً/دورة للمفتاح.
- `SyncService.php:82` — حفظ `close_blockers` داخل معاملة تُلغى بالاستثناء نفسه، فلا تصل للمشرف.
- `event_queue.dart:71` — طوابع زمنية بلا إزاحة منطقة، والخادم يفسّرها بمنطقته.
- `sync_engine.dart:263` — البصمة تُعاد من القرص وقت الرفع فتُلغي الغرض من حسابها وقت الالتقاط؛ استخدم `pending_media.sha256` المخزّن.
- `SlaSweepCommand.php:31` — يحمّل كل الزيارات غير المقفلة بلا تقسيم.
- `InventoryService.php:169` — `balance()` تجمع التاريخ كاملاً في كل نداء؛ منحنى تدهور حقيقي.
- `evidence_store.dart:152` — `pruneUploaded` لا يُستدعى أبداً؛ الصور تتراكم بلا سقف.
- `pubspec.yaml` — `connectivity_plus` معلن وغير مستخدم؛ لا مزامنة تلقائية عند عودة الشبكة.
- `main.dart:11` — الافتراضي `http` صريح؛ اجعله https وقيّد cleartext في الـmanifest.
- `SubcontractorService.php:75` — `order_no` بـ`max(id)+1` عرضة للتصادم.

---

## ترتيب التنفيذ ومعايير الاستلام

| الموجة | لماذا هذا الترتيب | معيار الاستلام |
|---|---|---|
| **0** | لا معنى لأي شيء آخر قبل أن يُقفل التطبيق زيارة | **اختبار تكامل واحد يعبر السيم:** تطبيق ← خادم، دورة كاملة offline، إقفال ناجح |
| **1** | ثقوب استغلالها فوري، وإصلاحها ساعات | اختبار سلبي لكل نقطة: فني يحاول ← 403 |
| **2** | فساد صامت لا يظهر إلا بعد فوات الأوان | حذف مدخل القاعدة من الأرشيف → `verify()` تفشل · اختبار تزامن يثبت عدم السالب |
| **3** | يظهر بعد التشغيل بأيام | نافذة 15:00–02:00 تعطي `dueAt` صحيحاً ولا تلوّن أحمر |
| **4** | يظهر في الميدان لا في المعمل | مهلة 30 ث تُنتج `NETWORK` · 401 يُخرج للدخول ولا يُفشِل الطابور |
| **5** | تحسينات | — |

---

## قاعدة الاختبار الملزمة

العيوب الحرجة الأربعة **كلها في السيم بين التطبيق والخادم**، وكلها مرّت من تحت 134 اختباراً لأن لا اختبار واحد عبر ذلك السيم.

**مطلوب قبل إغلاق الموجة 0:** اختبار تكامل يشغّل الخادم فعلياً ويقود عميل Dart حقيقياً عبره:

```
bootstrap → انتقالات offline → التقاط صور بأصولها → توقيع
→ sync واحدة → إقفال ناجح → PDF فيه الصور والتوقيع وزمن موقع صحيح
```

هذا الاختبار وحده كان سيمنع الأربعة.

**وقاعدة عامة:** أي اختبار يُنشئ صف `MediaFile` أو `ChecklistInstance` مباشرة بدل المرور بـ`media.register` **يخفي عيوباً ولا يكشفها**. راجع `VisitLifecycleTest::satisfyCloseGate` و`NotificationTest::completeVisit` وأعد كتابتهما على المسار الحقيقي.

---

## تقدير الجهد

| الموجة | ساعات |
|---|---|
| 0 — الجرح القاتل | 24–36 |
| 1 — الأمن | 6–10 |
| 2 — فساد البيانات | 16–24 |
| 3 — الحسابات | 10–16 |
| 4 — صلابة التطبيق | 12–18 |
| 5 — تشغيلية | 10–16 |
| اختبار السيم (إلزامي) | 12–16 |
| **الإجمالي** | **90–136 ساعة** |

على مبرمج واحد: **3–4 أسابيع**. وهذا رقم يستحق أن يُقال بصراحة — النظام لم يكن جاهزاً للتسليم رغم أن كل اختباراته كانت خضراء.
