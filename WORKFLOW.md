# سير العمل المعتمد

## تطبيق الفني: الأعمال الإضافية

1. ينشئ الفني الطلب أثناء تنفيذ الزيارة، ويضيف البنود والكميات وأسعار الوحدات.
2. يعيد الخادم حساب الإجمالي والضريبة ويرسل الطلب لبوابة العميل.
3. يمنع إقفال الزيارة ما دام القرار معلقًا، ثم تصل الموافقة أو الرفض للفني.

## دورة RFQ

1. تنشئ المشتريات طلب أسعار ببنود وموعد رد ومستودع وجهة.
2. تسجل عروض الموردين وتُقارن بالإجمالي ومدة التوريد.
3. الترسية المقفلة تنشئ أمر شراء واحدًا وتحفظ العرض الفائز.

## المسارات والفروع والتنبؤ

- يرسل تطبيق الفني موقعه عند الانطلاق، ويحدث الخادم ETA بمسار حي أو بديل محلي.
- حدود الفرع تُفرض في استعلامات الخادم وليست مجرد إخفاء عناصر واجهة.
- النموذج التنبؤي يتدرب أسبوعيًا بعد بلوغ العينة الدنيا ويحفظ الإصدار والمقاييس ودرجة خطر كل أصل.

## تعريف الإنجاز

1. ربط المتطلب بمسار واضح: لوحة المشرف أو تطبيق الفني أو بوابة العميل.
2. بناء مخطط البيانات والقيود والفهارس أولًا.
3. وضع قاعدة العمل في Service قابلة للاختبار.
4. فرض الصلاحية والملكية في الخادم ثم إضافة الواجهة المتجاوبة.
5. تدقيق الحركة الحساسة وإضافة idempotency للحركات المالية والمخزنية.
6. إضافة اختبار قبول ثم تشغيل المجموعة كاملة.
7. تحديث حالة التنفيذ وسجل التغييرات.

## التشغيل اليومي الآلي

| التكرار | المهمة | الناتج |
|---|---|---|
| كل 5 دقائق | `darak:notifications-run` | تفريغ صندوق التنبيهات |
| كل 10 دقائق | `darak:sla-sweep` | تنبيه خطر SLA |
| كل 15 دقيقة | `darak:reassign-unavailable` | إعادة توزيع الغياب/التعطل |
| 02:30 | `darak:backup` | نسخة احتياطية متحققة |
| 05:00 | `darak:maintenance-generate` | زيارات الوقاية |
| 06:00 | `darak:replenishment-scan` | طلبات التوريد |
| 07:00 | `darak:compliance-alerts` | تنبيهات وثائق الموظفين والسيارات والصيانة |
| 07:30 | `darak:renewal-offers --days=45` | عروض التجديد |
| 08:00 | `darak:commercial-alerts` | المتأخرات والتنبيهات |
| 23:30 | `darak:commissions-generate` | استحقاقات العمولات |

## مسارات الأعمال

- عرض → إصدار ومراجعة → إرسال → اعتماد العميل → عقد → دفعات → تحصيل وإيصال.
- خطة وقائية → زيارة → توزيع وطريق ومخزون → تنفيذ → تقرير → تقييم أو اعتراض.
- انخفاض رصيد → طلب توريد → شراء → استلام متتبع → تحميل سيارة → حجز وصرف → جرد.
- عطل متكرر → بلاغ فشل → مورد ودفعة → صحة الأصل → تشخيص أو استبدال → معرفة موثقة.
- خصم/تسوية → طلب مبرر → اعتماد أول → اعتماد ثانٍ مستقل → تطبيق وتدقيق.
- وثيقة موظف/سيارة → إصدار نشط → تنبيهات 90/60/30/7 → تجديد → أرشفة الإصدار السابق.
- طلب إجازة → تحقق من الرصيد والتداخل → اعتماد → غياب فني → إعادة توزيع الزيارات.
- عطل/صيانة سيارة → أمر → اعتماد → توقف وإعادة توزيع → إصلاح وتكلفة → صيانة قادمة.
- فحص سيارة يومي → عداد وقائمة سلامة وصورة → نجاح أو إيقاف السيارة وإعادة التوزيع.

## أوامر التحقق

```powershell
cd D:\darak_pro\backend
..\_tools\php\php.exe vendor\bin\pint --dirty
..\_tools\php\php.exe artisan route:list --except-vendor
..\_tools\php\php.exe artisan darak:audit-verify
..\_tools\php\php.exe artisan test --compact
```
